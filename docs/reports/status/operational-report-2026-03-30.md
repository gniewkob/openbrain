# Operational Report: 2026-03-30

## Status Summary: Green 🟢

The OpenBrain Unified platform is healthy. All core CRUD operations, MCP transport, and maintenance tasks are performing within expected parameters. Today's session focused on resolving the remaining duplicate noise in append-only domains and completing the tenancy column migration.

## Achievements

### 1. Governance-Safe Duplicate Remediation
Implemented a new remediation path for exact duplicates in the `corporate` (append-only) domain.
- **Problem**: 3 pairs of exact duplicates were blocked from mutation by the append-only policy, causing recurring noise in `maintain(dry_run=true)` and retrieval results.
- **Solution**: Introduced `status="duplicate"` and `duplicate_of` metadata. 
- **Outcome**: 
  - Canonical records remain `active`.
  - Duplicates are logically marked without destructive mutation.
  - Retrieval layer (search, find, list) now automatically excludes `duplicate` records.
  - Audit trail is preserved.
  - Total active duplicate candidates reduced to 0.

### 2. Tenancy Infrastructure Hardening
Completed the migration of `tenant_id` from JSONB metadata to a first-class relational column.
- **Migration**: Added `tenant_id` column with index. Backfilled existing data from metadata.
- **Integrity**: Updated `_to_record` and `_to_out` to prefer the dedicated column with metadata fallback.
- **Security**: Hardened access control tests to ensure tenancy and domain scoping are strictly enforced for both standard and administrative users.

### 3. Monitoring & Guardrails
- **Pre-seeded Metrics**: `TelemetryRegistry` now pre-seeds all known counters to 0, ensuring Prometheus series exist from first start.
- **Compose Guardrails**: Added CI checks to prevent insecure configurations (default passwords) in `PUBLIC_MODE`.
- **Metrics Bridge**: Deployed a lightweight bridge to allow Prometheus to scrape metrics with internal authentication.

## Metrics Snapshot (Post-Remediation)

| Metric | Value |
|--------|-------|
| Active Memories | 189 |
| Duplicate Candidates | 0 |
| Superseded Records | 42 |
| Remediated Duplicates | 3 |
| Search Zero-Hit Rate | < 2% |

## Regressions & Fixed Issues
- Fixed a bug where `brain_sync_check` could return records outside the user's administrative domain scope.
- Fixed a bug where `find_memories_v1` would include `duplicate` status records in LLM grounding packs.

## Engineering Backlog Update
- **Next Up**: Multi-tenant metrics isolation.
- **Next Up**: Persistent telemetry backend.

---
*Report generated by Gemini CLI Remediation Agent*
