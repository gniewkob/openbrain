global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files:
  - /etc/prometheus/openbrain-alerts.yml

scrape_configs:
  - job_name: openbrain-unified
    metrics_path: /metrics
    static_configs:
      - targets:
          - unified-server:80
